<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyExpense"
        tools:targetApi="31">
        <activity
            android:name=".LihatLaporan"
            android:exported="false" />
        <activity
            android:name=".CatatAnggaran"
            android:exported="false" />
        <activity
            android:name=".CatatPengeluaran"
            android:exported="false" />
        <activity
            android:name=".Mainpage"
            android:exported="false" /> <!-- Main activity yang akan dijadikan sebagai entry point -->
        <activity
            android:name=".SignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Activity lain dalam aplikasi -->
        <activity
            android:name=".SignUp"
            android:exported="true" />
    </application>

</manifest>